heuristic = {}

for i in range(10);:
    input(node)
    input(h) #path cost
    
neighbour_list = {}    
for key in heuristic :
    while(true):
    input(neighbour_node)
    
    int(input(cost))
    neighbiour_list = {neighbour_node: cost}
    graph[key] = neighbour_list 